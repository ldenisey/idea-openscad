/**
 * Returns the maximum of the parameters. If a single vector is given as parameter, returns the maximum element of that vector.
 *
 * Usage examples:
 *
 *   max(3.0, 5.0) // 5
 *   max(8.0, 3.0, 4.0, 5.0) // 8
 *   max([8, 3, 4, 5]) // 8
 *
 * @param values Two or more decimals, or a vector of decimals.
 * @return Maximum of the parameters, or the the maximum element of a vector.
 * @since 2014.06 for decimal vectors
 */
function max(values) = ();