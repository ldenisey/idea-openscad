/**
 * Mathematical signum function.
 *
 * Usage examples:
 *
 *     sign(-5.0); // 1.0
 *     sign(0); // 0.0
 *     sign(8.0); // 1.0
 *
 * @param number Signed decimal value.
 * @return Unit value that extracts the sign of x.
 */
function sign(number) = ();