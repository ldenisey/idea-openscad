/**
 * Mathematical arcsine, or inverse sine, expressed in degrees.
 *
 * @param number Decimal sine in the interval [-1, 1].
 * @return Inverse sine of x expressed in degrees.
 */
function asin(number) = ();