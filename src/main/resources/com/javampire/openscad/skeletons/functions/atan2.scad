/**
 * Mathematical two-argument atan function, taking y as its first argument.
 *
 * @param number_y Opposite side length.
 * @param number_x Adjacent side length.
 * @return Principal value of the arc tangent of y/x, expressed in degrees.
 */
function atan2(number_y, number_x) = ();