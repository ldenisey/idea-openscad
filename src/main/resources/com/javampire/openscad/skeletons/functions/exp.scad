/**
 * Mathematical exp function.
 *
 * Usage example:
 *
 *    echo(exp(1),exp(ln(3)*4));  // produces ECHO: 2.71828, 81
 *
 * @param number Signed decimal number.
 * @return Base-e exponential function of x, which is the number e raised to the power x.
 */
function exp(number) = ();