/**
 * Returns the minimum of the parameters. If a single vector is given as parameter, returns the minimum element of that vector.
 *
 * Usage examples:
 *
 *   min(3.0,5.0) // 3
 *   min(8.0,3.0,4.0,5.0) // 3
 *   min([8,3,4,5]) // 3
 *
 * @param values Two or more decimals, or a vector of decimals.
 * @return Minimum of the parameters, or the the minimum element of a vector.
 * @since 2014.06 for decimal vectors
 */
function min(values) = ();