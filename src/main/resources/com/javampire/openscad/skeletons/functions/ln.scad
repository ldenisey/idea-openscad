/**
 * Mathematical natural logarithm.
 *
 * Usage example:
 *
 *    echo(ln(4.4),ln(-4.4),ln(0));  // produces ECHO: 1.486, nan, -inf
 *
 * @param number Positive decimal number.
 * @return Base-e (natural) logarithm of x.
 */
function ln(number) = ();