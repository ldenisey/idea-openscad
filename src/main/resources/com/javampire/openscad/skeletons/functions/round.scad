/**
 * Returns the greatest or least integer part, respectively, if the numeric input is positive or negative.
 *
 * Usage examples:
 *
 *     round(x.5) = x+1.
 *     round(x.49) = x.
 *     round(-(x.5)) = -(x+1).
 *     round(-(x.49)) = -x.
 *     round(5.4); //-> 5
 *     round(5.5); //-> 6
 *     round(5.6); //-> 6
 *
 * @param number Signed decimal value.
 * @return Greatest or least integer part, respectively, if the numeric input is positive or negative.
 */
function round(number) = ();