/**
 * Mathematical square root function.
 *
 * Usage examples:
 *
 *     translate([sqrt(100),0,0])sphere(100);
 *
 * @param number Positive decimal value.
 * @return Square root of number.
 */
function sqrt(number) = ();